use crate::backend::compiler::generator::Location;

/*
struct Computer {
    program_counter: u8,
    accumulator: i16,
    current_instruction_register: u8,
    memory_address_register: u8,
    memory: [Location; 100],
}

impl Computer {
    fn new() -> Self {
        Self {
            program_counter: 0,
            accumulator: 0,
            memory: [Location::new(0, Operand::Number(0)); 100],
        }
    }
}
*/
